import { style } from '@vanilla-extract/css';

export const resumeWorkExperienceItemSectionContainer = style({
  minWidth: 230,
  maxWidth: 230,
  height: 'min-content',
});

export const resumeWorkExperienceItemSectionLinkStyle = style({
  textDecoration: 'underline',
});

export const resumeWorkExperienceItemSectionDescriptionStyle = style({
  wordBreak: 'keep-all',
});
