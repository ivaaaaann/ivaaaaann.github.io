import { style } from '@vanilla-extract/css';
import { vars } from '../../../styles/vars.css';

export const careerPerformanceContainer = style({
  borderBottom: `1px solid ${vars.color.border}`,
  padding: '16px',
});
